/docs/mock/pwa-context-convo.txt
/docs/mock/pwa-context-*.txt

# Feature-builder working notes (not tracked)
copilot/features/

# Design references and explorations are working material, not shipped
# product code; they stay local to whoever is iterating on them.
/design_reference/
/design_exploration/
/character_designs/
/new_designs/

# Personal AI-agent tooling ports (Codex/Copilot prompt mirrors of the
# ~/.claude skills); local scratch config, not shipped product code.
/.github/prompts/

# Python caches and bytecode
__pycache__/
*.py[cod]
*$py.class

# Virtual environments
.venv/
venv/
env/

# Build and packaging artifacts
build/
dist/
*.egg-info/
pip-wheel-metadata/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node
node_modules/
coverage/
*.tsbuildinfo
.vite/
npm-debug.log*

# Secrets, never committed
.env
.env.local
.env.*.local

# OS cruft
.DS_Store

# Claude Code per-machine permission overrides (CLAUDE.md itself IS committed)
.claude/settings.local.json

# Capacitor native build output (Task 2+). android/ and ios/ PROJECT FILES are
# committed once `cap add` runs; only generated build output is excluded
# (capacitor-builder skill's Sync and Build Workflow section).
android/app/build/
android/.gradle/
android/.idea/
android/local.properties
ios/App/Pods/
ios/App/build/
**/DerivedData/
xcuserdata/
*.xcuserstate
