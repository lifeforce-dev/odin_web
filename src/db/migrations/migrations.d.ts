// Hand-written types for the generated migrations.js bundle (drizzle-kit,
// driver 'expo'). drizzle-kit only appends to this folder on generate, so
// this file survives regeneration.
import type { MigrationBundle } from '../migrate';

declare const bundle: MigrationBundle;
export default bundle;
